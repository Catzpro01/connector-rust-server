# 02: Hybrid Cargo Workspace Scaffold and VPS Build Pipeline

Type: task
Status: unclaimed
Blocked by: 

## Question
Menyiapkan struktur workspace Cargo multi-crate (`crates/n8n-core`, `crates/n8n-nodes`, `crates/n8n-cli`), mengonfigurasi dependensi inti (`serde`, `tokio`, `tracing`), serta menguji alur delegasi build otomatis ke tab VPS via `connector-cli tab new --vps` sesuai aturan ADR 0001–0003.
